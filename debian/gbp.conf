[DEFAULT]
# the default branch for the debian patch:
debian-branch = debian-development
# the default tag formats used:
upstream-tag = v%(version)s
upstream-branch = upstream-master
debian-tag = debian/%(version)s
submodules = True
